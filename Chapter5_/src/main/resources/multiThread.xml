<?xml version="1.0" encoding="UTF-8" ?>
<suite name="multiThread" parallel="methods" thread-count="3">
    <test name="demo1">
        <!--
        tests级别：不同的test tag下的测试用例可以在不同的线程中执行，但一个test tag下的测试用例只能在同一个线程中执行。
        methods级别：所有用例都可以在不同的线程下执行。
        classs：相同的class tag 下的测试用例只能在同一个线程中执行。
        yhread-count：最大并发线程数
        xml文件配置这种方式不能指定线程池，只有方法上才可以指定线程池。
        -->
        <classes name="1">
            <class name="com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>
    <test name="demo2">
        <classes name="2">
            <class name="com.course.testng.multiThread.MultiThreadOnXml"/>
        </classes>
    </test>
</suite>